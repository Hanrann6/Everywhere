const theme = {
  ewhaGreen: '#00462a',
  subGreen: '25691F',
  black: '#000000',
  gray: '#979797',
  white: '#ffffff',
};

export default theme;

/* 
${({ theme }) => theme.ewhaGreen};
${({ theme }) => theme.subGreen;
${({ theme }) => theme.black};
${({ theme }) => theme.gray};
${({ theme }) => theme.white;
*/
